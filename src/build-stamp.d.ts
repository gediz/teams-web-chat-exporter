// Injected by Vite `define` in wxt.config.ts. A per-build identifier
// (git short hash [+ -dirty] @ ISO build time) baked into every bundle so an
// export, log line, or diagnostic can be traced to the exact build that made it.
declare const __BUILD_STAMP__: string;
